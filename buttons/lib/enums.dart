/// This defines the location for the widget to be displayed like prefix and sufix
enum WidgetLocation { start, end }

/// This defines the tilt side for it to be on the right or left side
enum TiltSide {
  left,
  right,
}
